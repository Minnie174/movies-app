import React from "react";
import { Spin } from "antd";

import './spinner.css';

const Spinner = () => {
    return (
        <div>
            <Spin />
        </div>
    )
}

export default Spinner;